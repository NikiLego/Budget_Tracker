print("Hello, world!")

Salary = float(input("Enter your current monthly salary"))
Expense = float(input("Enter your current monthly expense"))

#float converts them from strings to decimals, because input only takes in strings

Balance = Salary - Expense 

print("your balance is: ", Balance )
